services:
  - type: web
    name: emory-hacks
    runtime: node
    plan: free
    buildCommand: npm ci; npm run build
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: PORT
        value: 8080
